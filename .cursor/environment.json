{
  "name": "LogLab",
  "install": "node -e \"const m=parseInt(process.versions.node,10); if (m<20) { console.error('Need Node 20+; found '+process.versions.node); process.exit(1); }\" && npm install",
  "terminals": [
    {
      "name": "frontend",
      "command": "npm run dev",
      "description": "Next.js on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 }
  ]
}
